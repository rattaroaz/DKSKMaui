@page "/logout"
@inject NavigationManager Navigation

@code {
    protected override async Task OnInitializedAsync()
    {
        // In MAUI, we don't need local storage for auth tokens
        // Just redirect to home page
        Navigation.NavigateTo("/", true);
    }
}